import { MinStack } from './MinStack'

it('can create instances', () => {
  const stack = new MinStack()
  expect(stack).toBeTruthy
})

it('can push', () => {
  const stack = new MinStack()
  expect(stack).toBeTruthy
})
